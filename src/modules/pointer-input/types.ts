export type Options = {
  /** If true, request pointer lock on first pointerdown. */
  pointerLock?: boolean;
  /** Prevent context menu on right-click over the element (default true). */
  disableContextMenu?: boolean;
};